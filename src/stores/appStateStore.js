// src/store/appStateStore.js
import { defineStore } from "pinia";

export const useAppStateStore = defineStore("appState", {
  state: () => ({
    // TODO: Define state variables to manage application-wide state
  }),
  actions: {
    // TODO: Implement actions to set and clear loading state
    // Example action: setLoading(loading: boolean)
  },
});
